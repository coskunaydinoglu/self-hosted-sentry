# Public hostname the mobile apps send crashes to (DNS -> this host).
EDGE_HOSTNAME=crash-ingest.example.com
# Pin to the same Relay version as the internal stack (.env RELAY_IMAGE there).
RELAY_IMAGE=ghcr.io/getsentry/relay:nightly
